{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}"
        }

    ],
    "go.toolsEnvVars": {
      "GOROOT": "/usr/local/go",
        "PATH": "/usr/local/go/bin:${env:PATH}"
    },
}